# Nginx
NGINX_HOST=symfony-docker.localhost

# TIMEZONE
TZ=Europe/Paris

# PostgreSQL
POSTGRES_DB=symfony
POSTGRES_USER=symfony
POSTGRES_PASSWORD=symfony

# Xdebug
# get containers IP address: echo $(docker network inspect bridge | grep Gateway | grep -o -E '[0-9\.]+')
XDEBUG_REMOTE_HOST=172.17.0.1
